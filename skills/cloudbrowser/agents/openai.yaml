interface:
  display_name: "CloudBrowser"
  short_description: "Finish a bounded browser task with page evidence"
  default_prompt: "Use $cloudbrowser to check my chosen web page, return evidence, and close the browser within my approved usage bound."
